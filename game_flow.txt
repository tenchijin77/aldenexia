## Aldenexia: Lightfall — Game Flow & System Reference
## Last updated: 2026-08-23
## Purpose: Running reference for how all systems connect, for troubleshooting and onboarding.

================================================================================
AUTOLOADS (Global Singletons — always in memory, any scene can access them)
================================================================================

Global       → Scripts/global.gd
  - Holds all live player state in Global.player_data (Dict)
  - Also holds Global.current_character_data, current_character_name
  - Loads xp_table.json and character_options.json at startup
  - Game time system: 10-month, 36-day/month, 6-day/week calendar; 30 real
    seconds = 1 game minute
  - save_player_data_to_file()  → user://saves/{name}_character_stats.json
  - load_player_data_from_file() → reads save, calls set_player_data()
  - grant_currency(field, amount) → adds copper/silver/gold/platinum
  - Signals: chat_state_changed, time_changed, day_changed, month_changed,
    currency_changed

GameLog      → Scripts/game_log.gd
  - Pure signal bus, no state.
  - GameLog.log_general(text) → emits general_message
  - GameLog.log_combat(text)  → emits combat_message
  - GameLog.set_autoattack(bool) → emits autoattack_changed
  - game_log_window.gd connects to all three and routes them to the correct tab.

Inventory    → Scripts/inventory_autoload.gd
  - basic_inventory[12] — character sheet carry slots (bags or items)
  - bag_contents["0"…"11"] — contents of each bag in the above slots
  - equipped{slot → item_dict or null} — 25 paperdoll equipment slots
  - Loads all item definitions from items.json on startup
  - equip_item() / unequip_item() — emits equipment_changed when slot changes
  - sync_to_global() — writes inventory_data back into Global.player_data
  - Signals: inventory_changed, equipment_changed

GlobalBackgroundMusic → Scenes/global_background_music.tscn
  - AudioStreamPlayer that auto-starts on main_menu and character_creation.
  - Stops automatically for any other scene.


================================================================================
SCENE FLOW (How the player moves between screens)
================================================================================

  main_menu.tscn
      │
      ├─ [New Character] → character_creation.tscn
      │       └─ [Begin] → saves {name}_character_stats.json
      │                  → Global.load_player_data_from_file()
      │                  → lumora_outskirts3d.tscn
      │
      └─ [Load Game] → instantiates load_game.tscn as overlay
              └─ [Load] → Global.set_player_data(parsed_json)
                        → lumora_outskirts3d.tscn (active 3D zone)

  lumora_outskirts3d.tscn (main gameplay)
      └─ [Escape → Save & Exit] → saves file, frees all CanvasLayer UI,
                                   returns to main_menu


================================================================================
LUMORA OUTSKIRTS 3D — WHAT LIVES IN THE SCENE
================================================================================

lumora_outskirts3d.tscn contains:
  - DirectionalLight3D + WorldEnvironment (procedurally generated sky —
    Background = Sky, ProceduralSkyMaterial)
  - CharacterBody3D (player3d.tscn) — the playable character
  - NavigationRegion3D, containing:
      - LumoraOutskirts — instanced res://zones/LumoraOutskirts.glb (the Blender
        level art: walls, floor, docks, camps, POI markers, etc.)
      - LumoraOutskirts_Collision — StaticBody3D with one trimesh
        CollisionShape3D per mesh in the .glb (14 as of 2026-08-23), regenerated
        via a headless Godot script any time the .glb is re-exported from
        Blender (see "3D LEVEL COLLISION PIPELINE" below). NOT auto-generated
        by Godot's importer — must be re-run manually after every Blender export.
  - monster_template instance (test skeleton spawn) + MobSpawner3D
  - campfire (Scenes/campfire.tscn) — Campfire.glb + flame/smoke GPUParticles3D +
    looping AudioStreamPlayer3D (max_distance=10.0) + WarmthArea (Area3D,
    SphereShape3D radius 4.0). campfire.gd logs "The warmth of the fire renews
    your spirit as you take shelter nearby." to the General tab once when the
    player (group "player") enters the area; resets on exit so it can fire
    again next visit.

Note: the Terrain3D addon is present in addons/ but is NOT used by the current
zone — the level is entirely the Blender-authored LumoraOutskirts.glb mesh.
lumora_outskirts.tscn (2D) is legacy and not the active gameplay scene.


--------------------------------------------------------------------------------
3D LEVEL COLLISION PIPELINE (must be re-run after every Blender re-export)
--------------------------------------------------------------------------------

Godot's glTF importer does NOT auto-generate physics collision from an
imported .glb unless mesh objects are named with a "-col"/"-colonly" suffix
convention in Blender (not currently used in this project). Collision for
LumoraOutskirts_Collision is instead generated by a one-off headless Godot
script pattern:
  1. Load res://zones/LumoraOutskirts.glb standalone, walk every MeshInstance3D.
  2. For each: mesh.create_trimesh_shape() (local-space vertex data) + a
     manually-computed global transform (walk the parent chain multiplying
     Node3D.transform — NOT Node3D.global_transform, which returns identity/
     errors on a node that was never added to a live SceneTree).
  3. Text-splice the generated sub_resource + node blocks directly into
     Scenes/lumora_outskirts3d.tscn (NOT a full PackedScene.pack()+save() —
     that was tried first and silently stripped ext_resource UIDs and failed to
     persist edits inside the instanced LumoraOutskirts sub-scene at all).
  4. Run `godot --headless --path . res://Scenes/lumora_outskirts3d.tscn
     --quit-after 60` and grep stderr for "det == 0" / "cannot be normalized" —
     both indicate a singular (non-invertible) transform on some mesh, which
     will corrupt player physics silently (see gotcha below).

GOTCHA — degenerate Blender scale on any axis breaks physics silently:
  If a Blender object has a 0 scale on any axis (common accidental result of
  flattening a mesh via scale instead of editing vertices — invisible in
  Blender if the mesh's local geometry already lies flat on that axis), the
  resulting transform matrix is singular. Godot's physics engine throws
  "Condition det == 0" trying to invert it and produces NaN contact normals
  every physics tick the player touches that shape — symptoms are a frozen
  character, a broken-looking camera, and rendering that looks wrong (all
  downstream of the corrupted CharacterBody3D transform, not real bugs in
  those systems). Remember Blender is Z-up; glTF/Godot are Y-up, so Blender's
  Z-axis scale/location maps to Godot's Y-axis — check the right field.


================================================================================
PLAYER SYSTEM (player3d.gd)
================================================================================

Class: Player3D extends CharacterBody3D
Group: "player"
Scene: Scenes/player3d.tscn (contains CameraRig → CameraController + Camera3D)

--- _ready() order ---
1. add_to_group("player")
2. Instantiate CombatNode and add as child
3. Connect Inventory.equipment_changed → _on_equipment_changed()
4. load_faction_standing()       — reads Data/player_faction.json
5. _load_spell_cache()           — reads Data/player_spells.json +
                                   Data/player_skills.json into Dicts
6. load_player_data_from_global()— reads Global.player_data, loads inventory,
                                   applies equipment to CombatNode
7. _spawn_hud()                  — instantiates four HUD scenes on scene root,
                                   tags them "game_hud"

--- Movement ---
  WASD / Arrow keys: move_forward, move_backward, strafe_left, strafe_right,
                     turn_left, turn_right
  Shift (toggle_run): toggles run/walk
  R (toggle_autorun): keeps moving forward until toggled again or move_backward
                     is pressed. Logs "Autorun enabled/disabled." to General tab.
  Ctrl (crouch): crouches
  Space (jump): costs stamina
  X (sit): toggles sitting (no movement, faster stamina regen; logs message)
  Stamina: sitting 20/s (fastest) > standing idle 10/s > walking 5/s (added
  2026-08-23 — walking previously neither drained nor regenerated) > running
  drains 10/s.
  Movement/jump/crouch/run-toggle/combat input is suppressed while any
  LineEdit has focus (chat box open) — checked via
  get_viewport().gui_get_focus_owner() is LineEdit in _physics_process, added
  once the chat box became usable while walking around, not just static NPC
  dialogue. Velocity decelerates smoothly to a stop instead of going stale.

--- Camera (camera_controller.gd) ---
  Child Node3D "CameraRig" on Player3D.
  F12 toggle → first press captures mouse and enables mouse-look;
               second press releases to free cursor (MOUSE_MODE_VISIBLE).
  Default mode: MOUSE_MODE_VISIBLE (free cursor).
  Mouse-look clamped to ±90° yaw (180° total left/right) and ±45° pitch
  (90° total up/down) — fixed 2026-08-23; yaw previously had no clamp at all
  and pitch was ±60° (120° total).
  Mouse wheel → zoom (2m–12m).
  Home key → cycle three modes:
    THIRD_PERSON_BEHIND (default, 6m back)
    THIRD_PERSON_ANGLED (tactical, 8m back / 5m up / 45°)
    FIRST_PERSON (eye level)

--- Targeting ---
  Tab          → tab_cycle_target() — sorts live monsters by distance, always
                 selects index 0 (nearest). Logs "Target: [name] — HP x/y (zm away)".
                 (Fixed 2026-08-23: "tab_target" and "target_closest" input
                 actions had their key bindings swapped — tab_target was on
                 Escape, target_closest [unused elsewhere] was on Tab. Swapped
                 back in project.godot; Escape is now only the pause menu again.)
  Ctrl+Tab     → "target_closest" action — bound but not yet wired to any
                 function (known gap). Landed on Ctrl+Tab after Escape (from
                 the swap fix) and Shift+Tab (conflicted with toggle_run,
                 which fires on bare Shift key-down) were both ruled out.
  Ctrl+Escape  → clear_target() — nulls current_target, clears the target
                 frame, turns off autoattack. Added 2026-08-23 so a misclick
                 can be undone without waiting out a Tab-cycle. The hardcoded
                 KEY_ESCAPE check in _unhandled_input (opens pause menu) is
                 guarded with `and not event.ctrl_pressed` so the two don't
                 both fire on Ctrl+Escape.
  Left-click   → PhysicsRayQueryParameters3D ray (150m) from camera through click
                 position. Only fires in MOUSE_MODE_VISIBLE. Selects clicked monster
                 AND triggers melee attack if in range (click = target + swing).
  _announce_target(node) — shared helper for tab and click; updates target frame
                 and logs target info to General tab.

--- Combat ---
  ` (backtick/tilde, attack_target action) → toggles autoattack; auto-attacks
                 current target each cooldown. (game_flow previously said "T" —
                 corrected 2026-08-23 to match the actual InputMap binding.)
  Spells       → Keys 1–9, 0, -, = fire action_bar_slots entries
  melee_attack input action exists in code but has no key bound (known gap).
  Attack sequence: calls CombatNode.resolve_attack() → full EQ-style roll chain
  All combat results routed through CombatLogFormatter for colored BBCode messages.

--- Spell Casting (cast_spell) ---
  Checks cooldown (_spell_cooldowns Dict), mana, target validity.
  Deducts mana, starts cooldown.
  Physical school (e.g. power_strike): base_damage + STR/2, mitigated by target AC,
    applied as physical (triggers monster resistances).
  Magical schools (arcane, fire, cold, etc.): CombatNode.calculate_spell_damage()
    adds INT-based arcane_power.
  Logs result via CombatLogFormatter.spell_damage() → GameLog.log_combat().
  spell_casting skill ticks on every successful cast.

--- Skill System ---
  skill_levels Dict loaded from player_data["skill_levels"] on game load.
  _tick_skill(skill_name): chance to increment skill on use; scales down near cap
    (252). Logs "You've become better at [skill]! (X)" on gain.
  _sync_weapon_skill(): reads equipped weapon's "skill" field, syncs
    combat_node.weapon_skill to match.
  Melee hits tick the equipped weapon skill (1h_slashing, 1h_piercing, etc.).
  Successful parry/dodge/riposte also tick their respective defensive skills.

--- Equipment → CombatNode sync ---
  Inventory.equipment_changed → _on_equipment_changed()
    → _apply_equipment_from_inventory()
    → reads Inventory.equipped, sums weapon_damage and gear_ac
    → sets CombatNode.weapon_damage + gear_ac, calls recalculate_derived_stats()
    → calls Global.save_player_data_to_file()

--- Vitals (Hunger / Thirst) ---
  satiety and thirst each decay by 1–2 every 60 real seconds.
  Below 25: regen penalty on HP and mana regen ticks.
  At 0: take damage each decay interval (cannot kill — floor at 1 HP).

--- HP / Mana Regen ---
  EQ-style 6-second tick (regen_timer in _process).
  Amounts come from CombatNode.get_derived_stat("hp_regen"/"mana_regen").
  3× multiplier applied while sitting. Troll racial trait adds +4 HP/tick.

--- Looting ---
  Right-click (in MOUSE_MODE_VISIBLE) → _try_loot_corpse()
  Searches "monsters" group for nearest DEAD monster within 5m (regardless of
  is_lootable, so an empty/already-looted corpse still resolves to a "nearest").
  If nearest.is_lootable → monster.open_loot_window().
  Else → GameLog.log_general("You search the corpse but find nothing upon it.")
  If no DEAD monster at all within 5m → prints "No lootable corpse within range."
  to the debug console only (not the in-game log).

--- Level Up (on_level_up) ---
  Called by monster3d.die() when XP threshold crossed.
  Syncs combat_node level, recalculates derived stats, restores HP and Mana to full.

--- UI Toggles ---
  Escape → pause menu (pause_menu.tscn)
  C      → character sheet (character_sheet.tscn)
  K      → abilities book (abilities_book.tscn) — game_flow previously said
           "I"; corrected 2026-08-23 to match the actual InputMap binding.
  B      → backpack (backpack_ui.tscn)
  All instantiated on scene root; freed when toggled off.
  Closing C/B restores MOUSE_MODE_VISIBLE (does not force capture).


================================================================================
COMBAT LOG FORMATTER (Scripts/combat_log_formatter.gd)
================================================================================

Class: CombatLogFormatter (class_name — globally available, no autoload)
All functions are static — call as CombatLogFormatter.player_attack(...).

  player_attack(result, target_desc, weapon_name, damage_type) → BBCode String
    - Randomizes verb from damage_type-keyed arrays (slashing/piercing/blunt/generic).
    - Second-person conjugation ("You slash").
    - Crit prefix added if is_crit.

  monster_attack(result, monster_desc, damage_type) → BBCode String
    - Third-person conjugation ("A hooded bandit slashes").

  spell_damage(caster, spell_name, target_desc, damage) → String
  spell_heal(caster, spell_name, target_desc, amount) → String
  spell_debuff(caster, spell_name, target_desc, stat, amount) → String
  begin_cast(caster) → String  ("You begin casting a spell.")
  death(killer, target_desc) → String

  damage_type_from_item(item: Dict) → String
    Maps item "skill" field to damage type (e.g. "1h_slashing" → "slashing").

  monster3d.get_damage_type() derives damage type from monster category.
  All combat messages in player3d.gd and monster3d.gd route through this class.


================================================================================
COMBAT NODE (combatnode.gd)
================================================================================

Class: CombatNode extends Node
Added as child to both Player3D and Monster.

--- 7 Base Stats ---
  strength, constitution, dexterity, intelligence, wisdom, charisma, luck

--- Derived Stats (recalculate_derived_stats, cached until _stats_dirty) ---
  max_hp     = 50 + con*10 + class_hp_bonus + gear_hp
  max_mana   = 30 + (int+wis)*5 + class_mana_bonus + gear_mana
  armor_class = 10 + gear_ac + dex/2 + class_ac_bonus + shield_bonus
  attack_rating = weapon_skill*2 + str + dex/2 + gear_atk
  crit_chance = 5 + (dex+luck)/2 + class_crit_bonus + gear_crit  (cap 60%)
  dodge = class_dodge_base + dex*0.5  (cap 50%)
  parry = class_parry_base + dex*0.3 + weapon_skill*0.1  (cap 50%)
  riposte = class_riposte_base + dex*0.2 + weapon_skill*0.1  (cap 100%)
  arcane_power = int*2 + gear_spell_power
  hp_regen = 2 + int(con/3)  (per 6s tick; ×3 while sitting)
  mana_regen = 1 + wis/5  (per 6s tick)
  carry_weight = 100 + str*2

--- resolve_attack(target: CombatNode) → Dictionary ---
  Full attack resolution in order:
  1. Miss check (roll vs hit_chance)
  2. Riposte check (target parries + ripostes → attacker takes damage)
  3. Parry check (target parries)
  4. Block check (target blocks, requires shield)
  5. Dodge check (target dodges)
  6. Hit → calculate_melee_damage() → apply_ac_mitigation() → target.current_hp -= dmg
  Returns: {result, damage, is_crit, message}

--- Other attack modes ---
  resolve_dual_wield_attack() — main + offhand (-10% hit)
  resolve_aetherfist_attack() — flurry: main + optional double (lv10+) + triple (lv15+)

--- Spell interrupt (interrupt_spell) ---
  Checks concentration roll against cast progress.
  On failure: deducts mana scaled to how far cast had progressed.

--- Class bonuses ---
  set_class(new_class) applies class-specific hp_bonus, ac_bonus, dodge_base,
  parry_base, riposte_base, concentration_base.
  Classes: Blademaster, Aetherfist, Shadowblade, Voidknight, Lightsworn,
           Woodstalker, Troubadour, Spiritweaver, Gravecaller, Runecaster,
           Arcanist, Chaosborn


================================================================================
MONSTER SYSTEM (monster3d.gd)
================================================================================

Class: Monster extends CharacterBody3D
Group: "monsters"
Scene: Scenes/monster_template.tscn (base) + individual [name].tscn

--- Loading ---
  _ready(): reads monster_name (from @export or get_monster_name() override)
  → load_monster_stats() reads Data/monsters.json
  → configures CombatNode via _configure_combat_node()
    (gear values back-calculate from desired HP/AC/ATK so base stats stay 0)
  Sets gear_atk = 15 + level*5 for hit rates of ~55-60% vs typical player AC.

--- State Machine ---
  IDLE    → random patrol chance; aggro if player in aggro_range (≤5m)
  PATROL  → moves to random patrol points; aggro if player spotted
  CHASE   → uses NavigationAgent3D to path to player; attack when in range
  ATTACK  → looks at player, calls perform_attack() on cooldown
  DEAD    → no further processing; triggers loot roll and despawn timer

  Aggro range: JSON values scaled by AGGRO_RANGE_SCALE=10.0, hard capped at
  MAX_AGGRO_DISTANCE=5.0m. Passive (÷3) and neutral (÷2) behavior type
  modifiers applied before the cap. Leash check uses same effective range.

  Gravity always applied for all states (IDLE/ATTACK also call move_and_slide
  with gravity) so monsters never float.

--- perform_attack() ---
  Calls CombatNode.resolve_attack(player.combat_node).
  Routes result through CombatLogFormatter.monster_attack() → GameLog.log_combat().
  Social monsters (is_social=true) call call_nearby_allies() — same faction
  monsters within aggro_range switch to CHASE.

--- XP Award on die() ---
  Finds player via group, adds xp_gain to Global.player_data["xp"].
  Logs "You gain X experience points. (current/next)".
  Level-up loop handles multiple levels in one kill: increments player_level,
  looks up next threshold from Global.xp_table, logs gold level-up message,
  calls player.on_level_up() to sync CombatNode and restore HP/Mana to full.

--- Loot on die() ---
  Rolls _load_loot_data() from Data/solgrave_expanse_loot.json.
  Rolls category_loot_tables[category] (e.g. "undead", "animal").
  Rolls zone_loot_tables for monster-specific drops by name.
  Results combined into pending_loot; is_lootable = true if non-empty.
  60s auto-despawn via await (guarded with is_inside_tree() check).

--- open_loot_window() ---
  Instantiates corpse_loot_window.tscn, calls setup(name, pending_loot).
  pending_loot is passed by reference — taking items depletes the array.
  Connects all_looted → _on_fully_looted() → 5s despawn.

--- Child Monster Classes ---
  Each extends Monster, overrides get_monster_name() to return the key used
  in monsters.json. Optionally overrides modify_damage() for type resistances.
  Example — skeleton3d.gd:
    piercing → 70% (resistant)
    blunt    → 130% (weak)
    fire     → 110% (weak)
  Pattern applies to all mobs in Scripts/: bat, rat, slime, snake, spider,
  goblin, ghost, bandit, bonegnawer, dune_scarab, dustwalker variants,
  khepri_beetle, lumora_outrider, mirage_phantom, mummy, sand_stinger,
  sandshifter, sunmaddened_wanderer, thornback_lizard, wild_dog.


================================================================================
MOB SPAWNER 3D (Scripts/mob_spawner3d.gd)
================================================================================

Class: Node3D script attached to a MobSpawner3D node in lumora_outskirts3d.tscn.

  - Reads Data/lumora_outskirts_spawns.json on _ready().
  - Always loads monster_template.tscn (CharacterBody3D); sets mob.monster_name
    BEFORE add_child() so Monster._ready() auto-loads JSON stats.
  - Per spawn entry: manages respawn_time cooldowns, max_active counts,
    and spawn_radius scatter around the spawn point position.
  - VALID_MOB_TYPES: rat, snake, slime, spider, bat, skeleton, bandit, goblin, ghost.
  - lumora_outskirts_spawns.json: 10 entries with real 3D [x, y, z] coords (Y=2
    for terrain drop): rat×2, snake×2, slime×2, spider×2, skeleton, bandit.


================================================================================
HUD SYSTEM
================================================================================

All four HUD scenes are spawned by player3d._spawn_hud() onto the scene root
and tagged in group "game_hud". pause_menu._on_save_and_exit() frees all
CanvasLayer nodes from the root (covers game_hud and any open modal windows).

player_frame.tscn / player_frame.gd
  - CanvasLayer, polls player's CombatNode every _process frame.
  - Shows HP (red), Mana (blue), Stamina (yellow) bars + current/max values.
  - Bottom-left screen area.

target_frame.tscn / target_frame.gd
  - CanvasLayer, group "target_frame".
  - Updated by player3d._announce_target(node) when target changes.
  - EQ-style con coloring (updates live in _process if player levels up):
      Red ≥6 levels above, Orange ≥4, Yellow ≥2, White ≥-1,
      Blue ≥-3, Green ≥-5, Grey (trivial).
  - _get_display_name(): checks monster_description first (strips leading
    article, capitalizes), falls back to monster_name, then get_monster_name().
  - Shows target name, level, HP bar.

game_log_window.tscn / game_log_window.gd
  - CanvasLayer, two RichTextLabel tabs: General and Combat.
  - Connects GameLog.general_message → General tab.
  - Connects GameLog.combat_message → Combat tab.
  - Every line in both tabs is prefixed with a "[HH:MM AM/PM]" local-time
    timestamp, applied centrally in _append() (added 2026-08-23) — covers all
    existing log traffic (combat, quests, system messages), not just chat.
  - LineEdit at the bottom of the panel (added 2026-08-23) — was output-only
    before. Enter submits: text starting with "/" routes to
    _handle_slash_command() (currently: "/loc" prints player X/Y/Z to General
    tab); anything else posts as "You say, '...'" to General tab.
    (The NPC dialogue chat_window.gd has its own separate "/loc" handler too —
    same command, different input box, since that's where it was first asked for.)
  - Red flashing dot (⬤) top-right: visible when autoattack is ON,
    animated by Tween, driven by GameLog.autoattack_changed.
  - Draggable via "Chat" title bar; position saved to Global.player_data
    ["ui_positions"]["chat"] and persisted on mouse release.
  - Resizable: ◢ handle in bottom-right corner; minimum 180×80 enforced.
    Size saved to ui_positions["chat"] alongside position.
  - Right-click title bar → PopupMenu with font sizes: 10 11 12 13 14 16 18.
    Active size check-marked; selection applies instantly to both log tabs.
    Font size persisted to ui_positions["chat_font_size"].

action_bar.tscn / action_bar.gd
  - CanvasLayer, 12 slots (keys 1–9, 0, -, =), built entirely in _ready().
  - Polls player.action_bar_slots to show slot contents (spell "S" / skill "K").
  - Polls player._spell_cooldowns / _skill_cooldowns for cooldown overlays.
  - Mouse-clickable: left-clicking a filled slot fires the ability same as the
    number key; brief blue highlight flash for feedback.
    Slot mouse_filter = STOP so clicks land on the slot, not the bar.
  - Drag-and-drop from abilities_book to assign slots.
  - Draggable panel; position saved to Global.player_data["ui_positions"]
    ["action_bar"].


================================================================================
MODAL UI (toggled on demand, NOT tagged "game_hud")
================================================================================

pause_menu.gd (Scenes/pause_menu.tscn)
  - Escape key → player3d._toggle_pause_menu() instantiates this on scene root.
  - CanvasLayer layer 20 with dim overlay.
  - Save Game: calls Global.save_player_data_to_file(), logs confirmation.
  - Save & Exit: saves, frees ALL CanvasLayer children from root (covers hud +
    any open modal), changes scene to main_menu.
  - Resume / Escape again: consumes input event then emits closed, queue_free().

backpack_ui.gd (Scenes/backpack_ui.tscn)
  - Toggle with B key from player3d.
  - Title bar "Backpack" with ✕ close button (also restores MOUSE_MODE_VISIBLE).
  - Shows bag contents across all bags in basic_inventory.
  - Reads Inventory.bag_contents, creates slot_button nodes dynamically.
  - Search bar filters visible slots by item name.
  - Connects Inventory.inventory_changed → refresh_backpack().
  - Draggable panel.

character_sheet.gd (Scenes/character_sheet.tscn)
  - Toggle with C key; player3d calls set_player(self) after opening.
  - Title bar "Character Sheet" with ✕ close button.
  - Live-updating via _process(): HP, Mana, Stamina, XP, and Level update in
    real time while the sheet is open. Equipment/inventory/currency already live
    via signals.
  - Purple XP progress bar displayed immediately below the XP label.
  - Sheet data includes live CombatNode values plus Global.player_data
    (xp, currency, equipment, creation date, playtime).

abilities_book.gd (Scenes/abilities_book.tscn)
  - Toggle with I key; player3d.toggle_abilities_book() passes self as player.
  - Two tabs:
      "Class Skills" — known spells; shows spell name, school badge, mana cost,
        recast time, description, and "drag to bar" hint. Drag via
        set_drag_forwarding → sends {"type":"spell","name":...} to action_bar slots.
        Empty-state message: "No class abilities known. Use a scroll to learn spells."
      "General Skills" — passive combat skills; shows skill name, current level
        (e.g. "10 / 252"), description, and green progress bar toward 252 cap.
  - Draggable panel.

corpse_loot_window.gd (Scenes/corpse_loot_window.tscn)
  - Opened by monster3d.open_loot_window().
  - pending_loot is a direct reference to monster's array — looting depletes it.
  - "Take" or right-click: _take(drop) → _apply_drop() → removes from array.
  - "Loot All": iterates all drops.
  - Currency drops: Global.grant_currency(type, qty).
  - Item drops: Inventory.add_to_basic_inventory(item_id).
  - When array is empty: emits all_looted → monster._on_fully_looted() → 5s despawn.
  - Non-currency items support drag-drop to inventory slots.
  - Sets MOUSE_MODE_VISIBLE on open; restores MOUSE_MODE_VISIBLE on close.

slot_button.gd (used by backpack_ui and corpse_loot_window)
  - Right-click ALWAYS opens an inspect popup (regardless of item type).
  - Popup shows: name, stat line (dmg/AC/value), description, lore.
  - Context buttons by item type:
      Equippable gear → "Equip" button (calls Inventory.equip_item())
      Scroll → "Learn" button (triggers scroll learning)
      Always → "Close" button
  - Scroll learning: checks teaches_spell; if already known logs a message;
    otherwise adds to player.known_spells, removes scroll from inventory, saves,
    logs gold "You have learned X!", refreshes open abilities book.


================================================================================
CHARACTER CREATION (character_creation.gd)
================================================================================

  1. Reads Global.character_options (loaded by Global._ready from
     Data/character_options.json) for race and class lists.
  2. Reads Data/class_restrictions.json to filter classes per race.
  3. Race selected → load_racial_stats() reads Data/racial_stats.json for
     base stats and resistances.
  4. Player distributes 4 bonus points across 7 stats.
  5. On Confirm: collects stats, builds character_data Dict:
       - known_spells: always starts empty [] — spells must be learned from scrolls.
       - known_skills: [] (skills are auto-loaded from player_data["skill_levels"])
       - skill_levels: build_starting_skill_levels() reads player_skills.json
         "class_skills" section; new characters receive numeric starting levels
         (e.g. Blademaster: 1h_slashing=10, parry=5).
       - action_bar_slots: empty (player drags spells from abilities book)
       - starting inventory: via build_starting_inventory()
           Slot 0: small_bag containing faded_note, 20x iron_rations,
                   20x water_flask, 5x torch
           Slot 1: scroll_case (bag_size 4) containing class scrolls
                   (Blademaster: scroll_of_power_strike, scroll_of_battle_shout)
           Slots 2+: class-appropriate armor + weapon
       - equipment: all empty slots (player equips manually)
     Saves to user://saves/{name}_character_stats.json.
  6. On Begin: Global.load_player_data_from_file() → scene change to
     lumora_outskirts3d.tscn.


================================================================================
SAVE / LOAD SYSTEM
================================================================================

Save file: user://saves/{character_name}_character_stats.json

Contents:
  player_name, player_class, player_race, player_level
  stats: {strength, constitution, dexterity, intelligence, wisdom, charisma, luck}
  current_hp, current_mana
  xp, xp_next_level
  copper, silver, gold, platinum
  satiety, thirst, current_stamina, max_stamina
  known_spells[], known_skills[], action_bar_slots[]
  skill_levels: {skill_name → level}
  equipment: {slot → item_id}      ← legacy, used for first-load compat only
  inventory_data: {basic_inventory, bag_contents, bank_storage, equipped}
  ui_positions: {chat: [...], action_bar: [...], chat_font_size: int}
  resistances: {acid, cold, fire, magic, psychic}
  character_creation: {real_time, game_time}
  playtime_seconds

Auto-saved when:
  - Any equipment slot changes (Inventory.equipment_changed signal)
  - action_bar or chat window is dragged and released
  - A spell is learned from a scroll
  - Save Game is pressed in pause menu
  - Save & Exit is pressed in pause menu

Load path:
  main_menu → load_game → Global.set_player_data() → scene change
  → player3d.load_player_data_from_global()
  → load_character_data() sets CombatNode stats
  → Inventory.load_inventory_data() restores all bags/equipment
  → _apply_equipment_from_inventory() syncs CombatNode weapon_damage + gear_ac
  → skill_levels Dict loaded, _sync_weapon_skill() applied


================================================================================
DATA FILES (res://Data/)
================================================================================

monsters.json           — all monster stats: health, speed, damage, armor_class,
                          level, aggro_range, behavior_type, xp_gain,
                          coin_modifier, is_social, faction, category,
                          description, animations
items.json              — all item definitions: name, type, slot, damage,
                          armor_class, stackable, bag_size, icon, etc.
                          Scroll items have type="scroll" and teaches_spell field.
                          scroll_case: bag_size=4 bag item for storing scrolls.
player_spells.json      — spell definitions: spell_id, spell_name, mana_cost,
                          damage, recast_time, target (enemy/self/group),
                          class_level_requirements
player_skills.json      — skill descriptions keyed by category then skill name;
                          class_skills: starting numeric levels per class;
                          skill_max: 252; skill_gain_base_chance: 0.15
character_options.json  — race/class data for character creation UI
                          (portraits, descriptions, lore, traits, penalties)
racial_stats.json       — base stats and resistances per race
class_restrictions.json — which races can play which classes
xp_table.json           — XP thresholds per level (max_level key)
solgrave_expanse_loot.json — loot tables: category_loot_tables (by category
                          string) and zone_loot_tables (by mob name)
player_faction.json     — starting faction standing values
lumora_outskirts_spawns.json — 3D spawn point definitions used by mob_spawner3d.gd
                          (10 entries: rat×2, snake×2, slime×2, spider×2,
                          skeleton, bandit; real [x,y,z] coords with Y=2)
*_spawns.json           — 2D spawn definitions (legacy, not used in 3D zone)
dialogue_*.json         — NPC dialogue trees per town/zone
factions.json           — world faction definitions
deities.json            — world deity definitions


================================================================================
KEY SIGNAL FLOWS
================================================================================

Equipping an item:
  Inventory.equip_item()
    → Inventory.equipment_changed emitted
    → player3d._on_equipment_changed()
    → _apply_equipment_from_inventory() → CombatNode updated
    → Global.save_player_data_to_file()

Logging a combat event:
  Any script calls GameLog.log_combat("text")
    → GameLog emits combat_message
    → game_log_window._on_combat() appends to Combat tab RichTextLabel

Toggling autoattack:
  player3d.handle_combat() detects "attack_target" input
    → GameLog.set_autoattack(bool)
    → game_log_window._on_autoattack_changed() shows/hides flashing dot

Monster dies and gets fully looted:
  corpse_loot_window emits all_looted
    → monster3d._on_fully_looted()
    → 5s await → loot_window.queue_free() + monster.queue_free()

Monster dies without looting (60s timeout):
  monster3d.die() → await 60s → if is_inside_tree(): queue_free()

Currency looted:
  corpse_loot_window._apply_drop()
    → Global.grant_currency(type, amount)
    → Global emits currency_changed  (no UI listener yet — future HUD work)

Monster kill → XP → level up:
  monster3d.die()
    → Global.player_data["xp"] += xp_gain
    → level-up loop checks xp_table thresholds
    → player.on_level_up() → CombatNode synced, HP/Mana restored to full

Spell learned from scroll:
  slot_button._on_learn_pressed()
    → player.known_spells.append(spell_id)
    → Inventory removes scroll
    → Global.save_player_data_to_file()
    → abilities_book refreshed if open


================================================================================
GROUPS
================================================================================

"player"      — Player3D node; used by monsters to find target and by HUD
               scripts to find the player in _process
"monsters"    — all Monster nodes; used by player for targeting, looting,
               and social aggro calls
"game_hud"    — player_frame, target_frame, game_log_window, action_bar;
               freed as a batch on Save & Exit (also frees all CanvasLayers)
"target_frame"— target_frame nodes; player calls set_target(node) on all


================================================================================
KNOWN GAPS / TODOs (as of 2026-08-23)
================================================================================

- Death animations not implemented (TODO in monster3d.die())
- currency_changed signal has no UI listener yet
- character_sheet.gd.old and lumora_outskirts.gd are legacy 2D leftovers
- player_religion and spellbook save files are planned but not implemented
- Faction file saves (charactername_player_faction.json) not yet wired up
- Active skill effects are all placeholder (use_skill logs but does nothing)
- Starting scrolls only implemented for Blademaster; other classes TBD
- Buff system not implemented
- Cast times (channeled spells) not implemented
- HIGH PRIORITY BUG (found 2026-08-23): "tab_target" input action is bound to
  Escape instead of Tab (and an unused "target_closest" action holds the Tab
  binding instead) — Tab currently does nothing, Escape does double duty as
  pause menu AND target-cycle. Needs the two bindings swapped in project.godot.
- melee_attack input action has no key bound.
- Window/display renders as a small centered box even when maximized when run
  via the editor's Play button — almost certainly Godot's "Embed Game" editor
  toggle (not a project.godot setting); see change list.txt's #1 to-do item
  (1920x1080 / borderless fullscreen) for the follow-up work.
- Reimporting a re-exported .glb does NOT overwrite already-extracted loose
  texture files of the same name in zones/ — delete stale ones first or Godot
  silently keeps using old-resolution textures.
- Pool water texture in LumoraOutskirts.glb is still 8192×8192 (16.5MB) —
  largest single texture in the zone, candidate for downscaling.
- 3D level collision must be manually regenerated (see "3D LEVEL COLLISION
  PIPELINE" above) any time LumoraOutskirts.glb is re-exported from Blender —
  Godot does not do this automatically.
